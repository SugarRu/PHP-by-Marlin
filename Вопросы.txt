Как именно это делается: пусть в форме есть инпут с атрибутом name="user". Тогда после отправки этой формы мы можем получить данные из этого инпута таким образом: $_GET['user'] (или $_POST['user'], или $_REQUEST['user']). - ЕСТЬ ЛИ РАЗНИЦА В КАВЫЧКАХ "user" и 'user'?

Решим предыдущую задачу так, чтобы пользователь не мог вводить теги и сломать нам сайт. Для этого при записи содержимого поля в переменную $city обработаем его (содержимое, то есть $_REQUEST['city']) функцией strip_tags, которая удалит теги (можно вместо нее обработать функцией htmlspecialchars): - ЧТО ЗП STRIP_TAGS И ЗАЧЕМ ОН НУЖЕН? 